/**
 * Google Analytics gtag stub - must load before GTM/analytics scripts.
 * Inline script moved to external file for strict CSP (no 'unsafe-inline').
 */
window.dataLayer = window.dataLayer || [];
function gtag() {
  dataLayer.push(arguments);
}
